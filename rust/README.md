# rust

Rust joins later, under the same lockstep tag and the same `.schema-version`
pin as the Python and TypeScript packages in this repo.

The likely generator is [`typify`](https://github.com/oxidecomputer/typify) or
[`progenitor`](https://github.com/oxidecomputer/progenitor), run over the same
SiennaSchemas OpenAPI specs the other two languages consume.

No code lives here yet.
